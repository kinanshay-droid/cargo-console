# Base URL of the logistics REST API described in LOVABLE_HANDOFF.md.
# Include the protocol, host, and any prefix (no trailing slash).
# Example: https://api.your-domain.com  or  http://localhost:4000
VITE_API_BASE_URL=
